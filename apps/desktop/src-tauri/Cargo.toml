[package]
name = "speedreader-desktop"
version = "0.0.1"
description = "Speedreader desktop shell"
edition = "2021"

[lib]
name = "speedreader_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
